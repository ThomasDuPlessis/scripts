# Basic Scripts for System utilites

Relies on the following packages:
    - libnotify (for vol)
    - (lemonbar)[https://github.com/LemonBoy/bar] (for bar)
    - pulse audio (for vol)
    - imagemagick (for wal)
    - dunst (As notification server)
